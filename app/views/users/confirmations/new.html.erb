<% @pageTitle = "Resend Confirmations Instructions | CAT Forum" %>
<% @PageDescription= "Resend Confirmations Instructions" %>
<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-12 col-md-offset-3 col-md-6 col-lg-offset-3 col-lg-6">
      <div class="thread-card text-center">
        <h2>Resend confirmation instructions</h2>

        <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post, class: 'form-group' }) do |f| %>
          <%= devise_error_messages! %>

          <p><%= f.label "Enter email to receive confirmation instructions" %></p>
          <p><%= f.email_field :email, autofocus: true, class: 'form-control' %></p>

          <p><%= f.submit "Resend confirmation instructions", class: 'btn btn-primary' %></p>
        <% end %>

        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>
